<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans   
		  http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
          http://www.springframework.org/schema/context   
          http://www.springframework.org/schema/context/spring-context-3.0.xsd">

	<!-- 生产者 -->
	<bean id="myProducer" class="com.taylor.mq.MyProducer" init-method="init" destroy-method="destroy" scope="singleton">
		<property name="producerGroup" value="${producer.producerGroup}" />
		<property name="namesrvAddr" value="${producer.namesrvAddr}" />
		<property name="instanceName" value="${producer.instanceName}" />
	</bean>

	<!-- 枚举变换 -->
	<bean id="first2offet" class="org.springframework.beans.factory.config.FieldRetrievingFactoryBean">
		<property name="staticField" value="com.alibaba.rocketmq.common.consumer.ConsumeFromWhere.CONSUME_FROM_FIRST_OFFSET" />
	</bean>
	<bean id="clustering" class="org.springframework.beans.factory.config.FieldRetrievingFactoryBean">
		<property name="staticField" value="com.alibaba.rocketmq.common.protocol.heartbeat.MessageModel.CLUSTERING" />
	</bean>
	<!-- 消费者 -->
	<bean id="myConsumer" class="com.taylor.mq.MyConsumer" init-method="init" destroy-method="destroy" scope="singleton">
		<property name="consumerGroup" value="${consumer.consumerGroup}" />
		<property name="namesrvAddr" value="${consumer.namesrvAddr}" />
		<property name="instanceName" value="${consumer.instanceName}" />
		<!-- 设置Consumer第一次启动是从队列头部开始消费还是队列尾部开始消费,如果非第一次启动，那么按照上次消费的位置继续消费 -->
		<property name="consumeFromWhere" ref="${consumer.consumeFromWhere}" />
		<!-- 设置为集群消费(区别于广播消费) -->
		<property name="messageModel" ref="${consumer.messageModel}" />
		<property name="concurrentlyMessageListener" ref="messageListener" />
		<property name="topic" value="MyTopic" />
		<property name="tag" value="MyTag" />
	</bean>

	<!-- 消息监听 -->
	<bean id="messageListener" class="com.taylor.mq.RocketMessageListener" scope="singleton" />
</beans>